

<h1>商品一覧</h1>

<%= link_to "新規登録", new_admin_item_path %>

<tr>
<th>商品id</th>
<th>商品名</th>
<th>税抜価格</th>
</tr>

</br>

<tbody>
<% @items.each do |item| %>
<tr>
 <td><%= item.id %></td>
 <td><%= link_to item.name, admin_item_path(item.id) %></td>
 <td><%= item.price %></td>
</tr>
<% end %>
</tbody>